name: Update Orca

on:
  workflow_dispatch:
  schedule:
    - cron: "43 7 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v4
        with:
          minimum_release_age: 24h
      - name: Update cask and API metadata
        env:
          GH_TOKEN: ${{ github.token }}
        run: mise run update:orca
